from backend.ai.prompt import AIP_SYSTEM_PROMPT, format_agent_input

def run_aip_loop(ember_zones, damage_zones, crews, shelters, hospitals, routes) -> list:
    """Stub — full implementation in Phase 4."""
    return []
